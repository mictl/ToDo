{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Aufgabe bearbeiten{% endblock %}

{% block body %}

    <div class="container">
        <h1>Aufgabe bearbeiten</h1>
        <div>
            {{ form_start(taskForm) }}
                {{ form_widget(taskForm) }}
                <button type="submit" class="btn btn-primary">Speichern</button>
            {{ form_end(taskForm) }}
        </div>
    </div>

{% endblock %}
